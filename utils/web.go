package utils

// GetHTMLContent return string html for an url
func GetHTMLContent(url string) {

	// response, err := http.Get(url)
	// if err != nil {
	// 	log.Fatal(err)
	// }
	// defer response.Body.Close()
	// htmlContent, err := html.Parse(response.Body)

	// fmt.Println(htmlContent.FirstChild.Data)
}
